

def fft(_x, _axis=None):
    """Return the Fast Fourier Transform of `x` at the given `axis`.

    Use a negative integer `-n` to specify the last `n` axes.
    If no `axis` is provided, this will return the n-dimensional FFT.
    """

    raise NotImplementedError


def ifft(_x, _axis=None):
    """Return the inverse Fast Fourier Transform of `x` at the given `axis`.

    Use a negative integer `-n` to specify the last `n` axes.
    If no `axis` is provided, this will return the n-dimensional IFFT.
    """

    raise NotImplementedError
